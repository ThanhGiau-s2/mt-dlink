name: CasestudiesIndex
id:   CasestudiesIndex
version: 1.0
author_name: Voyager
description: 導入事例トップ

applications:
  cms:
    menus:
      CasestudiesIndex:
        label: 導入事例トップ
      CasestudiesIndex:CasestudiesIndexEdit:
        label: 導入事例トップ編集
        mode: view
        args:
          _type: entry
          id: 215
        view:
          - blog
        condition: |
          sub {
            my $app = MT->instance;
            my $blog = $app->blog;
            my $blog_id = $blog->id || 0;
            if( $blog_id eq '4' ){
              return 1;
            }
            return 0;
          }